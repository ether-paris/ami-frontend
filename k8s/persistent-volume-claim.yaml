apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: ether
  name: ami-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi